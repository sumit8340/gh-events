name: Deployment Exercise
on: push # How to create multipe event [push, workflow_dispatch,]
         
         # specific branch target for run actions:-
         # push:
         #   branches:
         #     - main
         #     - 'dev-*'    # example- dev-new dev-this dev-this-is-new
         #     - 'feat/**'   # example- feat/new feat/new/button
         
         # Activity typse trigger:-
         #on: 
         #  pullrequest:
         #    types:
         #      - opened
         #      - closed
jobs:      
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v5
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Test code
        run: npm run test    
      - name: Build code
        run: npm run build
      - name: Deploy code
        run: echo "Deploying..."

